import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  inputOut: {
    flexDirection: 'row',
    alignItems: 'center',
    borderWidth: 1,
    borderRadius: 4,
    paddingHorizontal: 5,
  },
  inputIn: {
    height: 45,
    marginTop: 5,
  
  },
});