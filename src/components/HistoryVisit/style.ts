import {StyleSheet} from 'react-native';
export const styles = StyleSheet.create({
  main: {
    height: 200,
    flex: 1,
    alignItems: 'center',
    padding: 10,
  },
  cardList: {
    marginHorizontal: 5,
    borderRadius: 15,
    width: 100,
    height: '80%',
    alignItems: 'center',
    justifyContent: 'center',
    alignSelf: 'center',
    borderColor: 'red',
    borderWidth: 1,
  },
});
